# Build output
bin/
obj/
*.user

# Visual Studio
.vs/
*.suo

# Publish
publish/
*.pubxml.user

# Local notes / scratch
*.odt

# Cloudflare Wrangler
.wrangler/

# OS junk
Thumbs.db
.DS_Store

*.exe
bin/
obj/

*.tmp

# Heavy source art - stored locally only, not shipped and not part of the project.
# Anchored with a leading slash so it only ignores a /brand folder at the repo root.
/brand/

# PyMuPDF compression helper bundle (~55 MB of binaries: portable Python + PyMuPDF).
# Built locally by PdfHelper/fetch-pdfhelper.ps1 and deployed by the build's
# CopyPdfHelperOutput target. Only the small compress_pdf.py script is tracked in git.
/PdfHelper/bundle/
/PdfHelper/.venv/
/PdfHelper/.venv-build/
/brand/

# Python bytecode
__pycache__/
